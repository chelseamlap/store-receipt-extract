{
  "name": "store-receipt-extract",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome MV3 extension that extracts itemized Target + Costco order history for personal spend analysis.",
  "type": "module",
  "scripts": {
    "test": "node --test 'tests/**/*.test.js'"
  },
  "devDependencies": {
    "fake-indexeddb": "^6.0.0"
  }
}
